name: Wake Streamlit App

on:
  schedule:
    - cron: "0 23,5,11,17 * * *"   
  workflow_dispatch:

jobs:
  wake:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Streamlit App
        run: |
          curl -L --silent --show-error https://app-calculator2025-uwxvkhbsq2ndhvvgepocwh.streamlit.app
